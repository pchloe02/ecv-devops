services:
  jenkins:
    build: .
    container_name: jenkins-local
    user: root
    ports:
      - "8001:8080"
      - "50001:50000"
    volumes:
      - jenkins_home:/var/jenkins_home
    restart: unless-stopped

volumes:
  jenkins_home:
